## iimod host freshness/downgrade protection

- ✅ Kept registry `schemaVersion: 2` and its fields unchanged. Added independent `HOST_GENERATION=2` durable state at `$STATE/host/current.json` and immutable `$STATE/host/generations/<generation>-<contentId>/` bundles containing manifest, both QML assets, and sorted target/optional/full host `PatchInstance` records.
- ✅ New mutators install and preserve exact legacy `$STATE/lock` contents `1\n` (PID 1 fence) while still holding the released lock, then use `$STATE/mutation.lock.v2` through common `mutation_preflight`; new `doctor --rebuild-registry` is covered. Released read-only commands remain unaffected, while released mutators fail before host writes.
- ✅ Host comparison is monotonic and semver-independent: an older candidate reuses the validated installed bundle; equal generation/content reuses it; equal-generation content collision fails closed; a newer candidate persists/upgrades; corrupt or missing installed bundles never fall back to embedded assets. First migration adopts only exact live assets/fences for normal mutations and reserves explicit replacement of unknown live host for `reapply`.
- ✅ `install`, `commands`, `recovery`, `ops`, and `verify` use the selected `HostBundle` assets and patches. Canonical `contentId` covers generation, protocol, both QML assets, and sorted target/optional patch records. Descriptor/bundle writes use temp+sync+rename. Install rollback now covers host assets/sentinel/descriptor, registry/index, module payload, same-version store, stock/config/translations.
- ✅ `verify` distinguishes corrupt bundle, descriptor/sentinel mismatch, modified assets/imports, and host fence divergence. Regression coverage includes the exact legacy PID1 incident ordering, generation branches/collision, newer-host preservation, candidate upgrade, migration match/mismatch, asset/sentinel/import/fence/bundle tamper, module-only patch target, and wipe/reapply.
- ✅ CLI version bumped `1.1.0` to `1.2.0`; host generation remains independently `2`. No publish, tag, live install, or live mutation was performed.
- ✅ Committed the completed scope as six focused commits: `02c2f51` Modules workspace UI/contracts; `0384ae9` durable host generation and locking core; `2d774b4` mutation/recovery/verify integration; `9a49df8` host downgrade regressions; `c25ae72` verified release entry point; `a321511` README/AGENTS/paired skills. Other module worktree changes remain uncommitted and were excluded.
- ✅ Shared-tree final verification after independent review fixes: `cargo fmt --check` exit 0; unit tests 56/56; integration tests 21/21; `cargo clippy --all-targets -- -D warnings` exit 0; Modules UI contracts 11/11; release metadata contracts 14/14; publish-helper contracts 13/13; paired skills byte-identical; `git diff --check` clean. The earlier full `tools/release/publish.sh cli --allow-dirty` dry-run passed all-target clippy, tests, release build, and artifact verification for `iimod/v1.2.0` without creating a tag.
- ✅ Independent review found and fixed three guard defects. `mutation.lock.v2` now uses nonblocking kernel `flock` on a stable inode and releases only by closing the file; active locks are never unlinked. The permanent legacy fence is atomically published from a unique, fully written and synced temporary inode via hard-link no-replace, and existing non-`1\n` bytes fail closed unchanged. First migration validates full sentinel identity plus live fence `(index, version, normalized content)`. Descriptor and actual bundle loading reject future protocols with exit 10 before mutation.
- ✅ Red evidence was captured before each fix: an unlinking `Drop` let a third contender acquire a replacement lock; stale real fence headers across five targets let same-version install return 0, print `already installed`, and create a descriptor before `verify` failed 7; generation 3/protocol 2 was accepted and mutated state. Final focused regressions and the full 56+21 suite are green; adversarial re-review reports no remaining finding. Frozen source fingerprints: `hoststate.rs` `a2e74b66ba24d28d9f7357d96de72b1075e9204136268d0df0375bacb5682d82`, `registry.rs` `6eab3d22bb50557283cbddb4641506cc7f84d5d9a025c662ccbc7661371fc63b`. Residual boundary: `flock` is Linux/Unix-specific, matching the current Linux CLI target; privileged external unlink is outside the normal contender model.

## network_traffic 1.6.0 optional pktz backend


- ✅ Added `pktz --log` as the optional preferred per-app source with a mutually exclusive `pktz → nethogs -t -C -v 2 → ss -tinpH` fallback chain. IIMP v1 has no optional `requires.system` field, so nethogs remains the declared guaranteed fallback; runtime discovery tries `pktz` from `$PATH`, then `$HOME/go/bin/pktz`, then `$HOME/.local/bin/pktz`, covering graphical Quickshell sessions that do not inherit Fish PATH. The module never invokes sudo or mutates capabilities.
- ✅ `AppTrafficLogic.js` validates the exact upstream process NDJSON schema, accepts RFC3339Nano UTC timestamps, ignores connection records and upstream bps, and derives rates/accounting from process cumulative payload-byte deltas. First sight, counter shrink, collector restart, comm change, and observable frame disappearance establish a new baseline; existing v1 `appAcctState` and boot/today/month totals remain unchanged.
- ✅ NDJSON framing is conservative: a timestamp batch is committed only after the next different timestamp arrives. No timer guesses frame completion; EOF, active stop, and ownership handoff discard the unsealed final batch because upstream has no frame marker and may swallow stdout write errors. This loses at most one log tick rather than treating a partial frame as complete.
- ✅ Popup reports the active capability as `eBPF estimate`, `pcap`, or `TCP only`; settings, zh_TW/zh_CN translations, manifest description, and README explicitly label pktz best-effort and document payload-byte semantics, silent optional UDP RX/IPv6 probe degradation, PID-only identity limits, empty-output behavior, process-exit limits, and one-time external setcap setup. Module version is 1.6.0.
- ✅ Regression fixture and contracts cover exact NDJSON replay, malformed/null/boundary input, RFC3339Nano, direction, cumulative deltas, upstream bps underflow immunity, counter shrink, PID/comm disappearance, collector mutual exclusion/restart, FailedToStart fallback, user-facing best-effort labels, and handoff behavior. Focused Node tests pass 39/39; JS/Python syntax, JSON, diff-check, IIMP source/package validate/check pass.
- ✅ Codex review found and the implementation fixed a blocker where Quickshell 0.2.1 emits `runningChanged` but no `exited` on `QProcess::FailedToStart`: a guarded zero-delay check now starts nethogs only for that path, while normal exit cancels the check. Review also required direct best-effort labels in catalog/settings/popup; all user-facing surfaces and translations now comply.
- ✅ PR #10 passed CI and merged to `main` at `a7d0e839857ce85c5810c7838846426fbce667eb`. The documented publisher atomically pushed `module/network_traffic/v1.6.0`; release workflow `30425468087` and Pages workflow `30425579857` both succeeded. The canonical `network_traffic-1.6.0.iimod` SHA256 is `0b31c858f9e999feb5c734c1f7cfb777b68447e45a8940565645ac0e8683265d`, matching both `SHA256SUMS` and the production registry at `https://ii.n1cat.xyz/index.json`. Downloaded package checksum, validate, and check all pass.
- ✅ Ordinary-user live verification used the installed `$HOME/go/bin/pktz` with existing `cap_dac_read_search,cap_perfmon,cap_bpf=ep`; no sudo was invoked. `pktz --log` loaded eBPF and emitted process/connection NDJSON for TCP, UDP, and Spotify. The deterministic loopback smoke now passes its supported contract: required TCP RX plus TCP/UDP TX cumulative payload bytes; optional UDP RX is reported as coverage rather than required because the corresponding upstream probe can be unavailable.

## modules management redesign

- ✅ Audited the current `tools/iimod/assets/ModulesConfig.qml`, live end-4 settings shell/widgets, index projection schema, and current installed data.
- ✅ Confirmed the root UX problems are structural: fixed 600 px content width, paragraph-length list rows, competing metadata/actions, and module settings expanding inside the list.
- ✅ User confirmed the visual system must remain end-4 Quickshell. The redesign reuses `Appearance` tokens, Material Symbols, ripple controls, rounding, typography, and motion; mainstream plugin managers inform only the information architecture.
- ✅ Requirements/design specification written to `plans/modules-management-redesign.md`: prominent header, search/status filters, compact selectable module rows, stable wide list/detail workspace, narrow list-to-detail navigation, explicit status/error handling, and no protocol-field expansion for v1.
- ✅ Implementation complete in `tools/iimod/assets/ModulesConfig.qml` with focused contracts in `tools/iimod/tests/modules-config-ui.test.mjs`. The installed-only manager now has an end-4-native title/count header, localized search, four status filters, compact selectable rows, wide list/detail workspace, narrow detail navigation, explicit busy/error/Tier B states, host placement, and lazy module settings without inline list expansion.
- ✅ User-directed polish complete: selected filter text uses the dynamic `Appearance.colors.colOnPrimary` token paired with the dynamic primary background (no hardcoded colors), and the search field is single-line with `TextInput.AlignVCenter`.
- ✅ Verification complete: UI contracts 11/11; Qt 6 `qmlformat` parses the QML; Rust unit tests 46/46 and integration tests 13/13; `cargo fmt --check` and `git diff --check` pass; `iimod reapply` and `verify` report all eight modules plus host intact; live source/asset bytes match; Settings logs contain no Modules errors; user screenshot confirms the wide list/detail UI, dynamic dark selected-chip text, centered search field, and real `network_traffic` settings fragment without overlap.
- ⚠️ Concurrent-session incident documented in `plans/ISSUES.md`: a later `indicator_tools` reinstall using an older binary rewrote the embedded host asset to the old UI after obtaining the lock. The redesigned host was restored after the transaction. Follow-up P2 requires both queued lock waiting and host downgrade/freshness protection; all sessions should use the same worktree binary meanwhile.
- ⚠️ 2026-07-29 recurrence identified the exact local-command path: plain `iimod install /tmp/network_traffic-1.6.0.iimod` resolved to the stale 2026-07-27 `~/.local/bin/iimod`. It and the current worktree binaries all report `iimod 1.1.0`, but only the worktree builds contain the redesigned host asset. The transaction rewrote network_traffic, registry hashes, and the legacy 15,877-byte Modules UI at 08:48:33, so `verify` accepted the downgrade. The redesigned 43,975-byte host was restored with the explicit worktree debug binary; source/live SHA256 match, all modules/host are intact, and IPC returns `pong`. Permanent protection must combine new host freshness metadata with a registry schema gate that already-installed old binaries reject.

## animation_tuner implementation

- ✅ Continued the existing untracked v1.0.0 prototype as a Tier A window/settings module; no stock patch or live shell file was hand-edited.
- ✅ Confirmed host scope from the installed `Appearance.qml`: eight writable animation tokens expose duration, Bezier, and (where present) velocity; there is no host `precompute` property.
- ✅ Duration, Bezier, velocity, and explicit reduced motion apply through the writable token registry. Delay and Spring Lab are preview-only because Tier A cannot retrofit already-instantiated animation factories or replace inline stock `SpringAnimation` declarations safely.
- ✅ Removed delayed factory replacement after review proved it was partial and unsafe across unload; invalid/future configuration now restores startup token values before failing closed.
- ✅ Fixed Spring Lab replay to stop pending delay and reset without an animated return leg before starting the next real spring preview.
- ✅ Made dense token metadata, preset controls, Bezier legend/actions, preview controls, and final actions wrap at narrow settings widths.
- ✅ Clarified Reset scope, migration/invalid dirty state, real JsonAdapter wrapper format, velocity preview limits, search empty state, Replay semantics, and contextual coordinate accessibility.
- ✅ Verification complete after review fixes: Node contracts 21/21; every QML file parses with Qt 6 `qmlformat`; source and packed artifact validate/check; exact eight-token probes pass; package reinstall and `iimod verify` pass; IPC returns `pong`; invalid-schema live test logs stock restoration; the real settings app loads the fragment with a clean module log and screenshots confirm the token editor, Bezier canvas, stock/draft previews, Spring Lab, and final actions render without horizontal overflow.

## indicator_tools applet menus

- ✅ Task 9: Fixed Bluetooth Material menu empty labels, grouping, and device/profile pairing.
- ✅ Task 10: Implemented Wi-Fi Material renderer from nm-applet QsMenuHandle.
- ✅ Task 11: Integrated both renderers, bridge, manifest, README, and translations.
- ✅ Task 12: `iimod validate` and `iimod check` pass for v3.4.0; installed runtime matches repo; latest live log has no indicator errors.
- 🔄 Task 13: Codex review running.
- ⏳ Task 14: Handle verified Codex feedback.
- ✅ Task 15: Diagnosed and fixed opening animation jank without removing/simplifying animations.

## indicator_tools decisions and evidence

- Blueman and nm-applet D-Bus menus remain the sole action/state owners.
- Both stock indicators retain left-click behavior; right-click opens module-styled menus.
- Root cause of animation jank was synchronous Popup/QsMenuOpener/delegate creation plus model-driven top-level geometry changes during the existing animations; D-Bus latency itself was only ~7–10 ms.
- Quickshell 0.2.1 `LazyLoader.activeAsync` now incubates renderers off the click path.
- Popup anchoring uses a window-relative `itemRect` snapshot rather than retaining `anchor.item` across hot reload.
- Menu/layout height settles before the original opacity and elementResize animations start; animation durations/easing were not reduced or removed.
- User confirmed the result is much smoother.

## website documentation automation

- ✅ Task 8: Build-time module catalog, dynamic bilingual VitePress docs/navigation, and Pages rebuild-on-module-change shipped through PR #1.

## independent product releases

- ✅ Task 1: Created `feat/independent-releases` from `main` after PR #2 merged.
- ✅ Task 2: Integrated and lead-verified split module/CLI builders and verifiers. New dispatchers reject legacy `v*`; module tags are `module/<id>/v<semver>`, CLI tags are `iimod/v<semver>`, and each output has only its artifact, `SHA256SUMS`, and `RELEASE_NOTES.md`. Lead evidence: 13 contract tests; module `network_traffic` 1.5.0 full build/verify with canonical Pages origin; CLI 1.1.0 fmt, clippy, 46 unit tests, 13 integration tests, release build and verify; exact three-file outputs.
- ✅ Task 3: Integrated and lead-verified the fail-closed Pages projection. Interface: `npm --prefix site run releases:project -- --releases <page1.json,page2.json,...> --index-output <Pages index.json> --cli-output <Pages downloads/iimod/linux-x86_64>`. It accepts stable namespaced tags only, chooses highest semver, requires exact assets and GitHub HTTPS URLs, rejects invalid IIMP IDs, downloads and recomputes SHA256, optionally verifies GitHub digests, and writes only after all validation succeeds. Outputs are exact `{indexVersion:1,modules:{...}}`, CLI binary, and sibling `.sha256`. Lead evidence: 24/24 site tests and Node syntax checks.
- ⏳ Task 4: Split GitHub Actions and connect release changes to Pages deployment.
- ⏳ Task 5: Update release documentation and migration notes.
- ⏳ Task 6: Run full verification, including product dry-runs and site build.
- ⏳ Task 7: Codex review.
- ⏳ Task 8: Handle verified Codex feedback.

### Decisions

- Module tags are `module/<id>/v<semver>`; CLI tags are `iimod/v<semver>`.
- Every GitHub Release contains artifacts for exactly one independently versioned product and is created with `--latest=false`.
- The canonical module origin is `https://ii.n1cat.xyz/index.json`, retaining the exact `indexVersion: 1` schema accepted by `iimod update`.
- Pages selects the highest semver per namespaced product, requires the exact expected asset, downloads it, and recomputes SHA256 before publishing the aggregate index.
- CLI stable endpoints are `https://ii.n1cat.xyz/downloads/iimod/linux-x86_64` and `.sha256`.
- Legacy `v1.4.0` remains the repository-wide generic Latest compatibility bridge; no future product artifacts are added to it.
- No GitHub Releases will be published as part of implementation or verification without separate user approval.
- ✅ Task 4: Split GitHub Actions into module-only and CLI-only release workflows. Pages now rebuilds from `main` on successful product workflows and release `published`, `unpublished`, `edited`, or `deleted` events. `actionlint`, YAML parsing, and `bash -n` for every workflow `run` block pass.
- ✅ Task 5: Updated root/site READMEs and bilingual install/publish guides for namespaced product tags, canonical Pages endpoints, safe checksum-verified CLI installation, source-versus-release status, and the one-time legacy `v1.4.0` bridge.
- ✅ Task 6: Full verification passes: site tests 30/30; TypeScript, Vite production build, and VitePress build; lint exit 0 with four pre-existing Fast Refresh warnings; release contracts 13/13; all release shell syntax; actionlint; documented install snippets; `git diff --check`. Product dry-runs and real-artifact projection evidence remain recorded above.
- ✅ Task 7: Independent Opus/Codex-style review found two concrete issues: missing `release.unpublished` rebuild and projection accepting mixed-product asset sets.
- ✅ Task 8: Both review findings fixed and independently re-verified. Pages handles `unpublished`; projection requires exact product artifact plus `SHA256SUMS`, with mixed/missing/duplicate regression coverage. Reviewer confirmed withdrawal fallback/removal behavior and found no new concrete issues.

## battery_trend Image #7 usage analysis

- ✅ Root-cause diagnosis and module-only fix completed in an isolated worktree; no commit and no live-state modification.
- Root cause of the observed six em dashes: persisted history has 978 raw samples but no discharging class, no adjacent percentage drop, no discharge accumulator time/amount, and no closed discharge session. The recorded period is charging from about 3.6% to 100%, then AC/full, so drain rate, recent cycle equivalents, and average discharge depth are genuinely not computable yet.
- Additional defects fixed in the isolated worktree: reader instances discarded persisted current accumulators; 7/30-day analytics consumed only daily data instead of hourly/raw fallbacks; malformed persisted fields could propagate NaN into QML readiness checks.
- Changed files: `modules/battery_trend/BatteryLogic.qml`, `BatteryAnalytics.js`, `tests/BatteryAnalytics.test.mjs`, and `module.json` (1.2.1).
- Agent evidence: Node pure calculation tests 6/6 pass (normal, empty/null, exact boundary, tier dedup/fallback, raw reconstruction, malformed); Qt6 `qmllint` passes JS; Qt6 `qmlformat` parses `BatteryLogic.qml`; `iimod validate`/`check` pass for v1.2.1; `git diff --check` passes.
- Existing history immediately populates only metrics with evidence. The six discharge metrics require future unplugged samples; average depth additionally requires a closed discharge session of at least 10 minutes and at least 3% drop.

## indicator_tools right-click ownership fix

- ✅ Fixed the structural right-click conflict in v3.4.2 without changing stock left-click behavior or menu animations.
- Root cause: unlike self-contained bar modules such as `network_traffic`, indicator_tools nested a right-button `MouseArea` inside the stock `RippleButton`, creating two competing pointer owners for one gesture.
- The stock `RippleButton.altAction` is now the sole right-click owner. It maps the pointer into the visible Wi-Fi/Bluetooth icon Loader and dispatches to exactly one hover-only `AppletMenuBridge`; clicks elsewhere in the pill do nothing.
- Added `modules/indicator_tools/tests/right-click-contract.test.mjs`; observed 2/2 fail before the fix and 2/2 pass after it.
- Verification: directory and packed artifact validate/check pass for v3.4.4; Qt 6 `qmlformat` parses all changed QML; transactional `iimod install --allow-patches` upgraded the live module through v3.4.4; `iimod verify` reports all intact; IIMP IPC ping returns `pong`; reload log has no indicator_tools/AppletMenuBridge/routing errors.
- v3.4.3 made Wi-Fi and Bluetooth opening mutually exclusive, including cancellation of pending async opens. v3.4.4 moved both popups onto the stock `GlobalFocusGrab` dismiss lifecycle and removed per-bridge private focus grabs.
- Superseded by the latest live report: outside-click dismissal worked before the action-interactivity change, but actions and outside dismissal do not currently coexist. The current investigation treats popup/window layer, input routing, and focus-grab lifecycle as unresolved rather than accepting the earlier isolated confirmation.
- No commit created.

## indicator_tools 3.4.4 release

- ✅ Created and pushed branch `fix/indicator-tools-popup-lifecycle` with four focused commits; PR #5 is open, mergeable, and all checks pass. It has not been auto-merged.
- ✅ Added the minimal versioned stock fixture surface required for offline Tier B release validation; clean-clone module build/verify passes.
- ✅ Pushed annotated tag `module/indicator_tools/v3.4.4`; dereferenced remote tag SHA is `cb0178ce87b94659669a0a0ca12bf77c8796e2c6`.
- ✅ Module workflow `30324040167` passed and published exactly `indicator_tools-3.4.4.iimod` plus `SHA256SUMS`. Downloaded checksum verification passes; artifact SHA256 is `b449bbfcbadc40f9c31c55db50f0c00bac26c9f4944310a2eb116d5fee4d08fa`.
- ✅ Pages workflow `30324146842` passed; production index projects indicator_tools 3.4.4 with the matching immutable URL and SHA256.
- Until PR #5 is merged, production source catalog remains 3.4.1 and correctly keeps download disabled against release 3.4.4.

## short iimod installer

- ✅ Parallel agent implemented and lead independently reviewed a first-party POSIX installer at `site/public/install-iimod.sh`.
- Displayed command: `curl --fail --silent --show-error --location --proto '=https' --tlsv1.2 https://ii.n1cat.xyz/install-iimod.sh | sh`.
- Installer explains the Linux x86_64 product, `/usr/local/bin/iimod` destination, checksum behavior, and upcoming sudo password prompt before downloading or elevating. It rejects unsupported platforms before download/sudo, uses a restricted temporary directory, validates one 64-hex SHA256, fails closed, and cleans up.
- ✅ Created and pushed branch `fix/short-iimod-installer` with three focused commits; PR #6 is open and has not been auto-merged.
- Verification: focused installer tests 8/8; complete site tests 36/36; `sh -n`; checksum mismatch and unsupported-platform tests prove sudo is not invoked; TypeScript/Vite/VitePress build pass; lint exit 0 with four pre-existing warnings; source/dist installer scripts are byte-identical; diff-check clean.
- No CLI tag was created because the CLI binary/version is unchanged. PR #6 merge will trigger Pages deployment of the script and short command.

## indicator_tools 3.4.5 action regression

- 🔄 User reported that 3.4.4 closed the entire popup whenever any option was clicked, making actions appear non-interactive.
- Root cause in renderer code: Wi-Fi unconditionally called `entryButton.dismiss()` after every leaf `menuEntry.triggered()`; Bluetooth shared entries defaulted `dismissAfterTrigger` to true.
- Fix: actions now remain open after triggering so D-Bus model state can update; submenu navigation, mutual exclusion, and true outside-click dismissal remain unchanged. Module version is 3.4.5.
- Regression contract observed red on 3.4.4 and now passes 5/5. Directory/package validate/check, Qt 6 parsing, clean-clone release build/verify, live transactional upgrade, `iimod verify`, IPC ping, and reload logs pass.
- ⚠️ Latest live report supersedes the isolated contract conclusion: action buttons work, but outside-click dismissal no longer coexists with them. The current v3.4.5 branch is under root-cause investigation for popup/window layer, input routing, and focus-grab lifecycle; it is not considered behavior-complete.
- ✅ PR #7 is open, CLEAN, mergeable, and checks pass. Tag `module/indicator_tools/v3.4.5` is published; release workflow `30325989042` and Pages `30326256081` pass. Published package checksum verifies and production index projects 3.4.5 with SHA256 `031b7a426076954b40fa81979ea1ab5ca25edca83ef2ed4b32bd08f99cae15ef`.
- ⏳ Awaiting the current root-cause fix and renewed live confirmation of both action execution and outside-click dismissal together.

## concise iimod command follow-up

- PR #6 merged before later command-format pushes, so a clean main-based follow-up PR #8 was created.
- Display command is exactly `sh -c "$(curl -sS https://ii.n1cat.xyz/install-iimod.sh)"`; tests prohibit curl long options in the bootstrap command. Installer safety logic is unchanged.
- ✅ PR #8 is open, CLEAN, mergeable, and checks pass. Complete site tests 36/36 and build pass. Not auto-merged.


- ✅ Hotfix PR #4 merged at `e9221a2bf532f08f287a3d01ad735b557d06bec8`; CI and Pages run `30301431470` succeeded.
- ✅ Downloaded Pages artifact `8666709548` contains `index.json`, `downloads/iimod/linux-x86_64`, and its `.sha256` sibling.
- ✅ Production `index.json`, CLI binary, checksum, homepage, and referenced JavaScript bundle all return HTTP 200.
- ✅ Production index projects `network_traffic` 1.5.0 with SHA256 `71d32acf409d23208d6ffbbb4e9c2d99cbd122825c65e34269d16d6a8a906cbd`, matching the immutable module Release bytes.
- ✅ Production CLI SHA256 is `b4ad2bd87addb05f93ba14bc537e4d2684ceacef096315373cb58fce2afb109d`, matching the immutable `iimod/v1.1.0` Release and passing `sha256sum --check` with the stable checksum endpoint.
- ✅ Production bundle contains the readable multiline installer and no longer contains the escaped legacy `sh -c 'set -eu...` form.

## indicator_tools 3.4.7 live closure

- ✅ Latest live confirmation supersedes the pending 3.4.5 note: actions execute without closing, true outside clicks close, and the indicator popup no longer occupies the prior overlay tier.
- Root cause of action-click dismissal: `HyprlandFocusGrab.windows` received the `LazyLoader.item` proxy instead of the real popup `QsWindow`; compositor logs showed `focus cleared` before `releaseAction`. The bridge now records the real handle from `menuOpened(qsWindow)` and whitelists `[bar window, popup window]`.
- Known Wi-Fi/Blueman actions prefer end-4 `MaterialSymbol`; unknown applet-specific icons retain native `IconImage` fallback. Live screenshot confirms formerly blank Send files/Reconnect/Audio profiles/Manage icons render.
- Profiles and Disconnect are fixed-width compact buttons with labels independently overlaid at the button center; live original-resolution crop measures button/label centers within 1 px.
- Final evidence: indicator tests 7/7; directory/package validate/check and release build/verify pass; live v3.4.7 installed; outside-click screenshot closes popup; no debug instrumentation remains; `iimod verify` intact and IPC `pong`.
- ✅ Handoff hardening: bridge close and LazyLoader unload paths now clear the real popup focus handle, and the Quickshell-incompatible optional-call syntax was replaced with an explicit null guard after a live parser failure exposed it. The focused contract observed red before cleanup handling and passes 7/7 after the fix; Qt 6 parsing, worktree-debug `iimod` directory/package validate/check, diff-check, installed-payload comparison, IPC, integrity, and a clean post-reinstall Quickshell log pass.
- ⚠️ Concurrent UI work constraint: do not run system `iimod install/reapply` while `tools/iimod/assets/ModulesConfig.qml` is under active verification; the old embedded asset rewrites the live projection. Do not hand-edit live QML. After coordination, use `tools/iimod/target/debug/iimod` for any required transaction.

## network_traffic 1.5.1 per-app accounting

- ✅ Root causes fixed: nethogs lacked `-C` and omitted UDP/QUIC; expired day/month values survived reload; stale cumulative identities created PID-reuse spikes; pending/comm state survived process disappearance; expired `__other` buckets swallowed new-period traffic; busy/late `ps` callbacks could lose or misattribute pending bytes.
- Pure `AppTrafficLogic.js` now owns parsing, period repair/pruning, snapshot identity lifecycle, pending drain, and exact rate/accounting math. QML uses monitoring generations plus query serial/entry-id snapshots and a stop barrier to reject stale asynchronous `ps` results.
- Final evidence: 13 focused traffic tests plus 7 indicator tests, 20/20 integrated; red reversions proved missing `-C` and stale baselines; nethogs 0.8.8 confirms `-C` TCP+UDP and `-v 2` total bytes; package includes JS/tests; v1.5.1 directory/package gates pass.
- Live v1.5.1 uses `nethogs -t -C -v 2 -d 2`; reinstall preserved app count/settings and month totals increased normally instead of resetting; `__other` rolled to the current day; logs contain no module errors.

## animation_tuner research

- ✅ Research/design complete; no product or live files changed.
- Recommended v1 is a Tier A window/settings module that snapshots and overrides the 8 writable `Appearance.animation` tokens, with validated multi-segment Bezier editing, duration/velocity, factory-supported delay, real QML preview, presets, atomic JSON config, apply/revert, and strict reduced-motion snap.
- Honest v1 limits: delay applies only to factory-backed consumers; 27 readonly-curve references and 8 inline springs cannot be globally replaced by IIMP v1 insert-only patches. Spring Lab is preview/authoring-only until upstream exposes a writable motion registry.
- Proposed id/files, schema, UX, tests, restoration lifecycle, and acceptance criteria are available in the animation research agent report. Implementation has not started pending product-scope confirmation.

## screentime 1.2.1 Steam game names

- ✅ Fixed Steam windows such as `steam_app_438100` displaying as `Steam app 438100` instead of `VRChat`.
- Root cause: `Format.appName()` only converted separators into spaces. Quickshell 0.2.1 `DesktopEntries.heuristicLookup("steam_app_438100")` returns null even though the local `VRChat.desktop` entry is indexed.
- Fix: Steam app IDs now match the exact parsed desktop-entry command argument `steam://rungameid/<id>` and use that entry's local `name`; unknown IDs retain the readable fallback and no network lookup was added. Accounting keys and persisted history are unchanged.
- Regression evidence: `node --test modules/screentime/tests/format.test.mjs` failed with actual `Steam app 438100` before the fix and under a deliberate reversion, then passed 3/3 after restoration. Tests cover VRChat, generic app IDs, aggregate/empty/null inputs, and unknown Steam IDs.
- Verification: `qmlformat` parsing and `qmllint` pass; directory and packed v1.2.1 `iimod validate`/`check` pass; `git diff --check` passes; no debug instrumentation remains. Local package SHA256 is `ba38745ab63e5a6c076c32006dd27491fe4ca9236ab94b5d8061219468576048`.
- Live v1.2.1 is transactionally installed. `iimod verify` reports all modules intact, IIMP IPC returns `pong`, installed payload contains the lookup and regression test, and recent live logs contain no screentime/Format errors. A standalone `/tmp` formatter probe was not usable because it cannot reproduce the full stock `qs` singleton namespace; the direct Quickshell desktop-index probe did resolve exact command `steam://rungameid/438100` to `VRChat` after index load.
- ✅ Release branch `fix/screentime-steam-game-names` used three focused commits: `b14041a` regression tests, `6094b66` product fix/version/docs, and `1d7b377` minimal clean-release stock fixtures. Official clean-tree module build/verify and release contracts 13/13 pass. Independent Opus/Codex review found no concrete defects.
- ✅ PR #9 merged at `34b321ab9fa2c7e24da4bd6ad20083c39d3b4ab5`; PR CI and post-merge main CI passed. Annotated tag `module/screentime/v1.2.1` dereferences to that merge commit.
- ✅ Release workflow `30347859592` published the non-draft/non-prerelease GitHub Release with exactly `screentime-1.2.1.iimod` and `SHA256SUMS`. Downloaded checksum, package validate/check, and embedded canonical origin all pass; artifact SHA256 is `fe432c2dfba4901abeebbc805db7b522927245e51509ce258ff3719556617aad`.
- ✅ Pages workflow `30348067259` passed. Production `https://ii.n1cat.xyz/index.json` projects screentime 1.2.1 at the immutable GitHub asset URL with the same SHA256, and downloading through that indexed URL reproduces the exact bytes.

## network_traffic 1.5.2 unattributed accounting

- ✅ Root cause confirmed by red tests and live evidence: `parseNethogsLine()` applied the normal process identity rule to nethogs synthetic `unknown UDP/0/0` and `unknown TCP/0/0` rows, then rejected PID 0 before cumulative delta accounting. Those bytes were dropped; assigning them to Spotify would be unsupported because nethogs supplied no process identity.
- ✅ Exact synthetic TCP/UDP rows now map to stable `__unattributed_tcp` / `__unattributed_udp` identities. They retain nethogs sent/received direction, independent cumulative baselines, two-snapshot deltas, counter-shrink restart behavior, translations, and retention outside the 30 named-app limit. Malformed or unsupported unknown protocol rows fail closed.
- ✅ `statsPeriod` now defaults to `today`. Pure `migrateStatsPeriod()` performs the one-time schema-1 migration before the owner write: an unversioned legacy `boot` default becomes `today`, schema 1 preserves later deliberate `boot`, and future schemas preserve unknown period values unchanged. Existing `acctState` / `appAcctState` blobs remain intact.
- ✅ Multi-monitor bars elect one collector/writer. Secondary instances watch persisted blobs without accumulating or writing. Ownership handoff flushes both accounting blobs before dropping `ownerReady`; `blockWrites: true` routes `writeAdapter()` through Quickshell 0.2.1 `saveSync()`, and `atomicWrites: true` prevents partial JSON, so the new owner's delayed reload cannot race an unfinished old-owner write. The ownership Timer is explicitly held by `property var ownerReload: Timer`, not the `FileView.adapter` default property.
- ✅ Collector scope remains `nethogs -t -C -v 2 -d 2`; `ss -tinpH` remains TCP-only fallback. No privileged daemon, eBPF service, or unsupported attribution guess was added. Module version is 1.5.2 and README documents attributed/unattributed scope, Top 5 behavior, migration, and collector limits.
- ✅ Final source/package evidence: focused Node tests 22/22; `node --check`; Qt 6 `qmlformat` parse; debug-worktree `iimod` directory and package validate/check; packaged `ConfigLoader.qml` contains the blocking/atomic ownership barrier; `git diff --check`; no `[DEBUG-...]` instrumentation. Final package SHA256: `29cea02ca870db7601f04cf241ac8ab780da47b74b15dbcec9fd64315d0d0ae5`.
- ✅ Final live evidence used only the 2026-07-28 `tools/iimod/target/debug/iimod`: repaired the host projection after an accidental 2026-07-27 release-binary transaction, reinstalled 1.5.2, and confirmed the installed `ConfigLoader.qml` and current `ModulesConfig.qml` match source assets. Latest reload has no network_traffic/ConfigLoader errors; exactly one active nethogs collector uses `-t -C -v 2 -d 2`; a 65-second persisted snapshot shows Spotify and unattributed UDP increasing while unattributed TCP remains present and nondecreasing; period/schema/monitoring remain `today`/1/true; `iimod verify` reports host and all modules intact; IPC returns `pong`.
- ✅ Independent Opus review initially found six concrete pre-PR defects: stale non-owner config snapshots could overwrite accounting; an inactive startup timeout could launch `ss` beside a restarted nethogs collector; unresolved pending bytes were lost on handoff; counter shrink retained stale PID identity; restore could yield month below today; and future-schema fields were stripped by materialization. All six now have direct regressions and fixes: latest-raw field-intent config merge, owner-only accounting intents, future-field round-trip preservation, guarded collector stop/start barriers, exactly-once `__unattributed_process` finalization, discontinuity identity reset, and restore-time month/day clamping.
- ✅ Final source/package evidence after review fixes: focused Node tests 31/31; `node --check` for both pure JS helpers; Qt 6 `qmlformat` parses all module QML; debug-worktree `iimod` directory validate/check/suggest; `git diff --check`; no `[DEBUG-...]` instrumentation. Release artifact evidence will be regenerated from the dedicated clean branch before PR.
- ⚠️ Superseded config conclusion: latest-raw field merge plus atomic replacement prevents torn files but is not a cross-process transaction. A primary accounting flush and Settings write can both read V0, then the later whole-file write can restore stale accounting or discard the setting. This remained a blocking pre-PR finding after the six earlier fixes.
- ✅ True single-writer architecture implemented: only the primary bar owner can enter `ConfigLoader.queueChange()` or any `setText()` path. Settings and secondary bars are watched readers and send one JSON-serialized field intent through argv-safe `qs -c ii ipc --any-display call network_traffic setSetting`; the enabled primary `IpcHandler` is the only receiver/writer. `ConfigLogic.decodeSettingIntent()` allowlists seven public setting keys with enum, boolean, integer-boundary, and ping target validation; accounting blobs and `statsPeriodSchema` have no IPC setter. `ConfigRequest` is FIFO, coalesces queued values by key, and retries owner handoff failures at most eight times.
- ✅ Single-writer regression observed red before implementation (`decodeSettingIntent` and `ConfigRequest.qml` absent), then focused tests pass 32/32. Contracts prove Settings and secondary bars contain no direct adapter assignments, all sender arguments are argv entries rather than shell text, owner-ready gates every write, disabled readers use noncanonical handler targets, and handoff/accounting lifecycle regressions remain covered. JS syntax, directory `iimod validate`/`check`/`suggest`, diff/security/debug scans pass. Clean-branch official release build/verify and final independent review remain pending before push/PR.
- ✅ Clean-branch release candidate built from `fix/network-traffic-unattributed-accounting` at `4aa1b18c252bf8d0cd015aae6d1a34ca77538a29`: official `build-module.sh` and `verify-module.sh` pass source/package validate/check with canonical origin; release contracts pass 13/13; exact output is `network_traffic-1.5.2.iimod`, `SHA256SUMS`, and `RELEASE_NOTES.md`; package includes both pure helpers, `ConfigRequest.qml`, fixtures/tests, and single-writer guards. Artifact SHA256 is `8d8334f127f4cbd63bc95b576c6cbf75060c45ecc7a76753a66a66c9ffdb53d6`. Branch range remains exclusively `modules/network_traffic/**`; final independent review remains required before push/PR.
- ✅ Final independent Opus review found no verified blocking defect. It explicitly replayed the synthetic PID-0 cross-protocol discontinuity candidate: TCP `50/100 → 60/120` plus UDP `80/200 → 5/10` correctly retains TCP `tx=10, rx=20` while resetting only the UDP baseline. Focused tests remain 32/32 and branch scope/diff checks pass. Residual risk is recorded, not promoted to a finding: owner handoff, Settings→bar IPC, and multi-display switching were statically interleaved but not runtime fault-injected in a real multi-process Quickshell session.
- ✅ Dedicated branch pushed at `4aa1b18c252bf8d0cd015aae6d1a34ca77538a29`; PR #10 is open at `https://github.com/956zs/ii-modules/pull/10`, non-draft, `CLEAN` / `MERGEABLE`, and CI run `30366723744` passes. GitHub changed-file scope is exactly the 15 `modules/network_traffic/**` files. No merge, tag, GitHub Release, live install/reapply, or production index change was performed; `module/network_traffic/v1.5.2` remains absent pending a separately authorized post-merge release step.

## animation_tuner 1.0.1 selected-button contrast

- ✅ Root cause: end-4 `RippleButton` changes a toggled button background to dynamic `colPrimary`, but its default `StyledText` content does not change foreground. The selected token tab therefore retained a light foreground on the current light primary background. The explicit primary `Apply` action had the same incomplete token pairing.
- ✅ Red-capable contract added to `tests/ui-contract.test.mjs`: before the product edit, the focused suite passed 6/7 and failed specifically because the token delegate lacked `colOnPrimary`; after the fix it passes 7/7. The contract covers selected token tabs, explicit primary actions, and rejects literal hex/black/white color declarations.
- ✅ `settings.qml` now gives token tabs a dynamic `colOnPrimary` selected foreground and `colOnLayer1` unselected foreground; `Apply` uses `colOnPrimary`. No color is hardcoded. Module patch version is 1.0.1.
- ✅ Verification: all animation_tuner Node contracts 22/22 pass; Qt 6 `qmlformat` parses every module QML file; directory and packed package `iimod validate`/`check` pass; `suggest` reports no added capabilities or probes; `git diff --check` passes. Package SHA256 is `06a53bc789442cdce5c499640729004f3ee6788a4298d5311026a40feb08fdad`.
- ✅ `/tmp/animation_tuner-1.0.1.iimod` installed transactionally via the worktree `iimod`, upgrading live 1.0.0 to 1.0.1. `iimod verify` reports all modules and host intact, IPC returns `pong`, and installed `settings.qml`/`module.json` exactly match source. The Settings PID log contains no animation_tuner/QML/TypeError/ReferenceError; only the pre-existing missing `en_US.json` translation warning appears.
- ✅ Actual post-install visual closure completed after session recovery: user Image #11 confirms the redesigned wide Modules list/detail UI, Animation Tuner 1.0.1 detail fragment, and the selected `Element move` tab using the dynamic dark `colOnPrimary` foreground without layout overlap.

## release publish helper

- ✅ Added `tools/release/publish.sh` as the single local release entry point: `module <id>` and `cli` derive namespaced tags from manifest/Cargo versions, run full build+verify dry-runs by default, and require explicit `--push` for publication. `--allow-dirty` is dry-run-only.
- ✅ `--push` requires clean synchronized `main`, captures `EXPECTED_HEAD`, builds from a detached temporary worktree at that immutable commit, re-fetches/rechecks branch/tree/local HEAD/remote main/tags after verification, tags the explicit expected commit, and atomically pushes main+tag with a main lease. Build-time local/remote movement fails without leaving a tag; ignored local files never enter the release snapshot.
- ✅ Added isolated bare-repository publish contracts: 13/13 cover dry-run/no-tag, dirty gates, invalid input, main requirement, local/remote duplicate tags, local HEAD race, remote main race, ignored-file exclusion, and successful atomic push. Existing release contracts are 14/14 and now verify the Animation Tuner fixture surface.
- ✅ Added a minimal stock `Appearance.qml` fixture matching the writable runtime surface: seven tokens expose duration/type/bezierCurve/velocity and stock `scroll` intentionally omits velocity. Qt 6 parses the fixture; Animation Tuner's real canonical-origin build/package validate/check/verify dry-run passes.
- ✅ Real CLI dry-run passes fmt, clippy, 46 unit tests, 13 integration tests, release build, and release verification. Real module and CLI dry-runs leave both local and `origin` tag-set hashes unchanged.
- ✅ CI now runs `test-publish.sh`. README uses the single entry point as the daily workflow and labels raw tag commands as maintenance fallback. New root `AGENTS.md`, both project skills, and both portable skill copies document release and single live-mutation-owner rules; paired skill copies are byte-identical.
- ✅ Independent Opus review found three concrete pre-delivery issues (build/tag TOCTOU, ignored-file package divergence, shallow Appearance fixture); all three were fixed and covered by the final contracts. No commit, tag, push, GitHub Action, or GitHub Release was performed from the dirty feature branch.

## PR #8 installer safety follow-up

- ✅ Isolated branch `fix/pr8-installer-safety` contains three focused commits: `5836b24` gates bootstrap execution on a successful, non-empty complete response; `328cb3b` makes HUP/INT/TERM clean up and exit 129/130/143; `4f5f62c` restricts curl initial and redirected transport to HTTPS with TLS 1.2 minimum.
- ✅ Final lead verification at `4f5f62c6810db97894e7b2b7b656c798b4f84941`: site tests 46/46 pass; `npm --prefix site run build` succeeds; `sh -n site/public/install-iimod.sh` and range `git diff --check` pass. The build-generated unrelated `site/public/registry.json` change was inspected and removed, leaving the isolated worktree clean.
- ✅ Independent Opus security re-review confirms the prior HTTPS redirect downgrade finding is fixed: shell passes exact `--proto =https`, non-HTTPS initial/redirect transport fails closed, `--tlsv1.2` still permits TLS 1.3, and no new regression was found. Verdict: `READY`.
- ✅ The three commits were fast-forward pushed to PR #8 without rewriting history. GitHub reports head `4f5f62c`, `CLEAN` / `MERGEABLE`, and CI success. The PR body now documents the final safety behavior and 46/46 verification; no merge, tag, release, or `iimod` transaction was performed.

## PR #7 and #8 merge readiness

- ✅ GitHub currently has only PR #7 and PR #8 open against `main`. PR #7 changes only seven `modules/indicator_tools/**` files; PR #8 changes only five `site/**` files. File overlap is zero and responsibilities are independent.
- ✅ Both PRs report `CLEAN` / `MERGEABLE` with successful CI. `git merge-tree --write-tree` succeeds for both #7→#8 and #8→#7, so #7 can merge first without creating a conflict. Independent Opus review found no blocking correctness, regression, security, performance, or coverage issue.
- ✅ PR #7 body was updated from stale 3.4.5 claims to the actual 3.4.7 scope, 7/7 contracts, real-window focus lifecycle, icon/compact-layout fixes, and post-merge `module/indicator_tools/v3.4.7` release requirement. PR #8 body was updated to the final hardened bootstrap and signal behavior.
- Recommended order: #8 then #7, only to land the installer security hardening first; there is no technical dependency. Neither PR has been merged.

## clock_popup_slim 2.0.0 bar clock customizer

- ✅ Kept the technical id `clock_popup_slim` for in-place upgrades, renamed the visible product to Bar Clock Customizer / Bar 時鐘客製化, and bumped to 2.0.0 for the changed product/config contract.
- ✅ The horizontal stock `ClockWidget` is hidden by insert-only Tier B patches and replaced at the same layout position by module-owned `ClockBar`. Defaults show only `HH:mm` with zero horizontal padding; optional date, separator, content spacing, horizontal padding, and independent time/date size offsets are persisted in `~/.config/illogical-impulse/modules/clock_popup_slim.json` without changing global shell time settings.
- ✅ Qt time/date formats are validated and bounded; `:ss` enables second precision only when the seconds token is outside quoted literals. The vertical bar intentionally retains its stock stacked layout.
- ✅ Slim hover remains available as a live setting and defaults on, preserving v1 behavior; disabling it restores uptime and To Do without disabling the clock customizer.
- ✅ Verification: focused Node tests 7/7 (normal, empty/null, length/control-character, numeric boundary/error, quoted-second, manifest/config/translation contracts); Qt 6 `qmlformat` parses all module QML; directory and package `iimod validate`/`check` pass; isolated full-shell install, recomposed-stock QML parse, and `iimod verify` pass; catalog generation/check and focused site tests 10/10 pass; diff and secret scans are clean. Local package SHA256: `d3eea49cc89807daeb91af66b451fa973a505e39ea02856bcac6a5410f00def2`.
- ⏸ No live install/reapply was performed because concurrent work owns the shared Modules host projection; verification used isolated `IIMOD_II_ROOT` / state / shell-config roots instead.
- ✅ Live install later completed with explicit user approval using only `tools/iimod/target/debug/iimod`. A full live-tree/state/config simulation was diffed first; the real transaction upgraded 1.0.0 → 2.0.0 without changing any non-target module payload or the current `ModulesConfig.qml` asset. Expected host sentinel/import projection changes occurred, existing indicator_tools 3.4.7 fences were recomposed from the same registry (not downgraded), and final `iimod verify` reports every module plus host intact; IPC returns `pong`.
- ✅ Live config materialized as `showDate=false`, `timeFormat=HH:mm`, `horizontalPadding=0`, and `slimPopup=true`. The bar screenshot confirms a time-only display with no date/separator and no overlap; live QML parsing passes and logs contain no clock_popup_slim/ClockBar errors.
- ✅ Follow-up layout research confirmed the apparent center “bookends” are stock structure, not clock padding: `middleSection` is screen-centered and contains equal-width left/right groups around Workspaces. On the current 2560 px verbose horizontal bar each side is fixed at 360 px (responsive values 280 px at 1001–1200 and 190 px at <=1000), so changing the module's 0 px clock padding cannot remove that reserved space. Shrinking only the right group would shift Workspaces by about half the width delta; the safe clock-only option is to keep symmetric stock widths and optionally center ClockBar inside its available cell.
- ⚠️ Independent stock warning observed: `BarContent.qml` binds `padding: workspacesWidget.widgetPadding`, but current `Workspaces.qml` has no `widgetPadding`; live log reports `Unable to assign [undefined] to double`. This can affect only the workspace capsule's few-pixel padding, not the 360 px symmetric side widths.
- ✅ User corrected the product goal: the clock must not be centered; only the 10 Workspaces stay centered. v2.1.0 therefore adds `centerSideWidth` (280–360, default 280) and injects equal `Binding`s onto both stock center side groups using `Math.min(root.centerSideModuleWidth, customBarClock.centerSideWidth)`. On the current 2560 px verbose bar this reduces each side from 360 to 280 px (160 px total), while preserving stock 280/190 responsive caps and keeping the clock left-aligned in the right group.
- ✅ v2.1.0 verification: focused contracts 7/7 require both side bindings and identical formulas; all module QML parses; directory/package validate/check pass; full live-state simulation and real transactional upgrade preserve every non-target payload and all modules/host remain intact. Live config materialized `centerSideWidth: 280`; screenshot confirms both groups moved inward equally, the workspace center remains at screen x=1280, the clock remains left-aligned, and no overlap appears. No clock/width-binding log errors; source and live payload match after an explicit same-version transactional reinstall. Catalog tests 10/10 pass; package SHA256 is `2555ba1f1f8f1a1cd052a9223abf094b0da5ac047733f287d78f157fe37bc351`.
- ✅ Repository publication prepared from a clean `origin/main` clone so none of the primary dirty worktree's unrelated module/host changes were included. User selected the lifecycle label “experimental / may be deprecated”; manifest names/descriptions and README warning now expose that status without adding non-IIMP fields or disabling downloads. Clean branch `feat/clock-customizer-experimental` was pushed at `3a63769665cf33859d94d1a3595a7412641b1936` with four focused commits (runtime/settings, v2.1 manifest/catalog/translations, contracts, lifecycle docs). Push scope is exactly `modules/clock_popup_slim/**` plus generated `site/public/registry.json`; no tag, Release, PR, or merge was created. Clean-clone verification: module contracts 7/7, catalog tests 10/10, QML parse, directory/package validate/check, catalog current, diff/secret checks, and merge-tree against latest main pass. Experimental artifact SHA256: `1a93b40ec390befeb99d7c26219c6acff23a58136df1de2e7afc2b4004f52a94`.

## screentime 1.3.0 history and trends

- ✅ Added simple previous-day / next-day browsing across the latest 30 calendar days. The Daily tab drives the hero total, per-app ranking, AI summary, and today-only hourly/live state; missing dates render an explicit empty state.
- ✅ Added a separate Trends tab for the average across recorded complete days in the latest 7-day window, `N/7` coverage, comparison with the previous 7 complete days, a missing-aware 7-day column chart with average reference line, a missing-aware 30-day curve, and a Monday-to-Sunday × 24-hour heatmap over the latest 28 complete days.
- ✅ Daily history written from v1.3 onward includes strictly validated `hours[24]` in the existing single `histState` blob. Older records remain valid for daily totals/averages but are excluded from heatmap coverage; a persisted `hoursComplete` marker prevents a mid-day v1.3 upgrade from presenting only the post-upgrade tail as a complete hourly day. Malformed totals/hours are unknown rather than coerced to zero, while tracker-observed zero-usage days remain legitimate records.
- ✅ The heatmap exposes `N/28` coverage, peak text, hover readout, four-step Material primary legend, Tab focus, and arrow-key cell navigation. Its accessible name follows the selected weekday/hour/value, and bound Material color changes request a Canvas repaint while the panel remains open.
- ✅ Independent review confirmed four findings: cross-midnight tails were written to the previous hour bucket after new-day reset; malformed 30-day totals became observed zeroes; keyboard-selected heatmap cells were not announced; and theme changes left Canvas colors stale. All four are fixed and regression-covered. Review also confirmed no static horizontal overflow at 380px and refuted suspected errors in complete-day averaging, today exclusion, observed-zero persistence, and legacy-hour handling.
- ✅ Pure `HistoryLogic.js` covers date keys, month/year/leap boundaries, latest duplicate records, complete-period summaries, previous-period deltas, strict missing-value semantics, and 7×24 aggregation. UI/persistence contracts cover tab ownership, selected-day behavior, both historical fold paths, incomplete upgrade-day handling, chart presence masks, average reference line, heatmap accessibility/theme repaint, translations, docs, and semver.
- ✅ Verification: screentime Node tests pass 22/22; every module QML file parses with Qt 6 `qmlformat`; `HistoryLogic.js` compiles with `qmlcachegen`; JSON/catalog/diff/security checks pass; source and packed artifact `iimod validate`/`check` pass; `suggest` reports no new capability/probe. The updated release stock fixture covers screentime's seven declared stock surfaces; release script contracts pass 13/13, release contracts pass 14/14, and the release dry-run passes without creating a tag.
- ✅ Live installation used the current worktree's `iimod` 1.2.0 binary as the sole mutation owner. The pre-generation live host was inspected, then migrated through the official `iimod reapply` path to durable host generation 2 with its immutable bundle, authoritative descriptor, and permanent legacy fence intact; all eight installed modules and the host verify intact.
- ✅ Live loading exposed two Quickshell 0.2.1 JavaScript compatibility gaps that Node and static formatting did not catch: object spread in `HistoryLogic.js` prevented the screentime IPC target from registering, and `Array.prototype.flat()` in `HourHeatmap.qml` failed at runtime. Both were replaced with parser/runtime-compatible forms and regression contracts now forbid those constructs on the affected runtime paths.
- ✅ Final live verification confirms `iimp ping`, `screentime toggleDetails`, the 380px Daily/Trends panel, hourly heatmap rendering, retained settings/history, package/store/live payload equality, and a clean post-reload screentime log. Final package `/tmp/screentime-1.3.0.iimod` SHA256 is `709f5a37618b480e682bd36f853a478fc9f23c3e338ff2374e92657d315a5189`; all earlier 1.3.0 checksums are superseded.
- ✅ Published screentime 1.3.0 through the documented release path. PR [#12](https://github.com/956zs/ii-modules/pull/12) passed CI and merged to `main` at `1323da80244e5905eb8f070f1d6235dde6b87ea4`; `tools/release/publish.sh module screentime --push` created and atomically pushed annotated tag `module/screentime/v1.3.0`.
- ✅ GitHub Actions release run `30421931446` built, verified, and published the non-draft/non-prerelease Release with exactly `screentime-1.3.0.iimod` and `SHA256SUMS`. The downloaded canonical package has SHA256 `d03ebabb5ec81f2a776e7a55fe733f9820b3f3f87aff4fd5a9f5e31ec621e39b`, passes its checksum file plus iimod 1.2.0 `validate`/`check`, and contains manifest `screentime` version `1.3.0`.
- ✅ Pages projection run `30422053361` completed successfully. Public `https://ii.n1cat.xyz/index.json` now advertises screentime `1.3.0`, the canonical GitHub Release asset URL, and the same `d03ebabb…e39b` SHA256.

## iimod 1.2.0 release

- ✅ PR [#13](https://github.com/956zs/ii-modules/pull/13) passed CI and merged to `main` at `17e6e15c8ad8a66528536a4b1acf4b8ea5f2bbb2`. The focused release branch contained the responsive Modules host UI, durable monotonic host generation, permanent legacy mutation fence, stable-inode v2 mutation lock, integration regressions, and synchronized README/AGENTS/project+portable skills.
- ✅ `tools/release/publish.sh cli --push` completed from clean synchronized `main`, creating annotated tag `iimod/v1.2.0` (tag object `a4451e164dcc4e58e646c5f40bdb729107d088f0`, dereferenced commit `17e6e15c8ad8a66528536a4b1acf4b8ea5f2bbb2`). Pre-publication verification passed 56 unit tests, 21 integration tests, 11 Modules UI contracts, 13 publish contracts, formatting, all-target clippy with warnings denied, release build, and artifact verification.
- ✅ GitHub Actions release run `30422873935` published the non-draft/non-prerelease Release at `https://github.com/956zs/ii-modules/releases/tag/iimod/v1.2.0` with exactly `iimod-linux-x86_64` and `SHA256SUMS`. Downloaded `SHA256SUMS` verification passes, and the binary reports `iimod 1.2.0` after restoring its executable bit.
- ✅ Canonical CLI SHA256 is `ca6e3cd5a6e3167a84f49c84ae385db15df1543d57646279520ac07a2dee3120`. Pages projection run `30422927743` completed successfully; `https://ii.n1cat.xyz/downloads/iimod/linux-x86_64`, its `.sha256` endpoint, and the GitHub Release binary all resolve to that exact digest, and the Pages/Release binaries are byte-identical.

## Markdown documentation cleanup

- ✅ Audited all 45 initially untracked files. Kept 43 module sources, tests, release fixtures, and project documents; removed the accidental `727.txt` Claude transcript and generated `pktz-live-smoke.cpython-314.pyc`. Added `__pycache__/` and `*.py[cod]` to `.gitignore`; no remaining untracked file needs classification.
- ✅ Rewrote all eight module READMEs into scan-friendly user-first sections. The screenshot-like `network_traffic` dependency/implementation block is now split into backend, requirements, permissions, settings, accounting, persistence, limitations, migration, and maintainer sections. Corrected stale or overbroad claims across Network Traffic, Screen Time, Battery Trend, Memory Center, Clock Customizer, Indicator Tools, Animation Tuner, and ActiveWindow Fix.
- ✅ Added a mandatory Markdown documentation template and evidence rules to root `AGENTS.md`, then synchronized the requirement into both project and portable `ii-module-author` skill copies. The paired skills are byte-identical.
- ✅ Fixed contract drift found during documentation review: removed Network Traffic's unused stock `Graph.qml` probe, narrowed VPN exclusion wording, and changed the pktz smoke contract so TCP RX is required while optional UDP RX is reported as coverage. Regenerated the eight-module site catalog.
- ✅ Verification: `git diff --check`; paired skill comparison; all rewritten manifest JSON; Python smoke-test compile; Network Traffic 39/39, Screen Time 22/22, Animation Tuner 22/22, Clock Customizer 7/7, Indicator Tools 7/7; all eight modules pass source `iimod validate` and `check`; release contracts 14/14; catalog generation/check and site tests 36/36. No live install, mutation, commit, tag, or push was performed.
