import Quickshell

/*
 * Intentionally empty — the module's effect is one Tier B patch overriding a
 * buggy stock binding (see module.json). Self-retiring: when upstream fixes
 * the line, the anchor disappears and reapply auto-disables this module.
 */
Scope {}
